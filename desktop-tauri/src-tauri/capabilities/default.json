{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capabilities for VibeChat shell",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}